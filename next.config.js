/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'images.microcms-assets.io'],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
