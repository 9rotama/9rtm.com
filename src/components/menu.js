import * as React from "react"
import { Link } from "gatsby"

const Menu = ({ data }) => (
    <div>
        <Link to="/" >about me</Link>
        <Link to="/" >works</Link>
    </div>
) 

export default Menu